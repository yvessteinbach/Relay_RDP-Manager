{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capabilities for the Relay main window.",
  "windows": ["main"],
  "permissions": ["core:default", "core:app:allow-set-app-theme"]
}
